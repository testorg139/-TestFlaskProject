from setuptools import setup

with open('requirements.txt') as f:
    requirements = f.read().splitlines()

setup(
    name='TestFlaskProject',
    version='',
    packages=[''],
    url='',
    license='',
    author='kedarkulkarni',
    author_email='',
    description='TestFlaskProject',
    install_requires=requirements
)
